# --- Core ML / NLP ---
transformers>=4.40.0
torch>=2.2.0
datasets>=2.19.0
accelerate>=0.30.0
sentencepiece>=0.2.0          # needed for DeBERTa / many tokenizers
scikit-learn>=1.4.0           # metrics: f1, classification_report

# --- Embeddings / retrieval ---
sentence-transformers>=2.7.0

# --- Orchestration ---
langchain>=0.2.0
langchain-community>=0.2.0

# --- Data / utils ---
pandas>=2.2.0
numpy>=1.26.0
pyyaml>=6.0
tqdm>=4.66.0

# --- Optional demo UI ---
streamlit>=1.34.0

# NOTE: On Kaggle, torch/transformers/datasets are preinstalled. The notebook
# pins/installs only what's missing. For local CPU use, the defaults above work.
